<p style="color: green"><%= notice %></p>

<h1>Forms</h1>

<div id="forms">
  <% @forms.each do |form| %>
    <%= render form %>
    <p>
      <%= link_to "Show this form", form %>
    </p>
  <% end %>
</div>

<%= link_to "New form", new_form_path %>
